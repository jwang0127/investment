[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "investment-dashboard"
version = "0.1.0"
description = "量潮罗盘 A股每日大盘分析看板"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
